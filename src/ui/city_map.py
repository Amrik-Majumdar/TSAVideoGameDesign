"""
City map view (stubbed).

Placeholder for a potential city map showing listener locations.
Could visualize where listeners are tuning in from.
"""

import pygame
from src.game_state import GameState


def render_city_map(screen: pygame.Surface, state: GameState) -> None:
    """
    Render a city map visualization (stubbed).
    
    Args:
        screen: Pygame surface to render to
        state: Current game state
        
    Future feature: Show a map of the city with dots representing
    listeners, growing/shrinking based on listener count.
    """
    # Stub: Not implemented yet
    pass
